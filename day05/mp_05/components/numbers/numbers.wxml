<!--components/numbers/numbers.wxml-->
<view>{{numA}}+{{numB}}={{sum}}</view>
<button size="mini" type="primary" bind:tap="btnHandle2" data-step="{{1}}">numB+1</button>
<button size="mini" type="warn" bind:tap="btnHandle2" data-step="{{-1}}">numB-1</button>